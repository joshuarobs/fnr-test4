import { IoWarning } from 'react-icons/io5';

// A warning icon component that displays in red, used for form validation and error states
export const FormWarningIcon = () => {
  return <IoWarning className="h-4 w-4 text-red-500" />;
};
